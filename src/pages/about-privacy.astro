---

import MainGridLayout from "../layouts/MainGridLayout.astro";

import { getEntry } from "astro:content";
import { render } from "astro:content";
import Markdown from "@components/misc/Markdown.astro";

const aboutPost = await getEntry("spec", "about-privacy");

if (!aboutPost) {
	throw new Error("Privacy page content not found");
}

const { Content } = await render(aboutPost);
---
<MainGridLayout title="隐私政策" description="Bihrys's Blog 的隐私政策说明。包含网站数据收集、Cookie 使用、第三方服务（Cloudflare CDN、Giscus 评论、自建统计）等隐私保护措施详情。">
    <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32">
        <div class="card-base z-10 px-9 py-6 relative w-full ">
            <Markdown class="mt-2">
                <Content />
            </Markdown>
        </div>
    </div>
</MainGridLayout>
